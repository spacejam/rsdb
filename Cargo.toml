[package]
name = "rsdb"
version = "0.1.0"
authors = ["Tyler Neely <t@jujit.su>"]
edition = "2018"

[features]
shred_allocations_on_free = []
no_allocations_allowed = []

[dependencies]
libc = "0.2.98"
